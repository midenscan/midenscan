export enum BlocksSortBy {
  timestamp = "timestamp",
}

export enum AccountsSortBy {
  timestamp = "timestamp",
}

export enum TransactionsSortBy {
  commited_at = "commited_at",
}

export enum AccountUpdatesSortBy {
  commited_at = "commited_at",
}

export enum AccountVaultAssetsSortBy {
  faucet_id = "faucet_id",
}

export enum NoteAssetsSortBy {
  faucet_id = "faucet_id",
}

export enum NotesSortBy {
  commited_at = "commited_at",
}

export enum OrderBy {
  asc = "asc",
  desc = "desc",
}
